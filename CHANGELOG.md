## v0.5.0 (January 21, 2018)

- Added CLI feature to generate log messages in console or in bash scripts.
- `log.format(fn)` was renamed to `log.setFormat(fn)`.
- `log.level(level)` was renamed to `log.setLevel(level)`.

